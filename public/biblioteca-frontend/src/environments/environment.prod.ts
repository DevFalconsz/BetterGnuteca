/**
 * Configurações de ambiente para produção.
 * Estas configurações são usadas quando a aplicação é compilada para produção.
 */
export const environment = {
  production: true,
  apiUrl: 'http://localhost:8080'
};
